#include<stdio.h>
int factorial(int n){
    int result = 1,i;
    for(i=2;i<=n;i++){
        result *= i;
    }
    return result;
}
int main(){
    int num;
    printf("enter the number: ");
    scanf("%d",&num);
    printf("The factorial of %d is: %d ",num,factorial(num));
    return 0;
}
